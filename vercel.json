{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "if [ \"$OPENFRAME_BENCH\" = \"1\" ]; then pnpm build:bench; else pnpm build; fi",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "github": { "silent": true }
}
