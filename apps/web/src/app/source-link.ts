/**
 * Where this deployment's source can be obtained.
 *
 * This is not decoration. OpenFrame is under the AGPL, whose section 13 says
 * that if you MODIFY it and let people use it over a network, you must offer
 * those users the source of your modified version. The link in the status bar
 * is how that offer is made.
 *
 * **If you fork OpenFrame and host it, change this to point at your fork.**
 * Leaving it pointing here would offer users source that is not the source they
 * are actually running, which is the one thing section 13 exists to prevent.
 */
export const SOURCE_URL = 'https://github.com/Muqtadaa/open-frame'
