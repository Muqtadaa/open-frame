{
  "name": "@openframe/mcp",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "OpenFrame as a room peer for agents: joins a board over the same socket the web app uses and changes it through the same dispatcher.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json --noEmit",
    "cli": "tsx src/cli.ts",
    "start": "tsx src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "@openframe/collab": "workspace:*",
    "@openframe/core": "workspace:*",
    "@supabase/supabase-js": "^2.116.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "tsx": "^4.20.6",
    "typescript": "6.0.3",
    "vitest": "^5.0.1"
  }
}
