{
  "name": "@openframe/rooms",
  "version": "0.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "description": "The board room: one Cloudflare Durable Object per board, holding the sockets and the storage that @openframe/collab deliberately knows nothing about.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@openframe/collab": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "6.0.3",
    "vitest": "^5.0.1",
    "wrangler": "^4.135.0"
  }
}
